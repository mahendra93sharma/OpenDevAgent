This folder is a code project. Run its test suite, find every failing test,
and fix the underlying code (not the tests, unless a test itself is wrong —
justify in DONE.md if so). Spawn parallel subagents to fix independent
failures concurrently. Verify: full test suite passes with exit code 0.
Deliver: passing tests + FIXES.md listing each failure, root cause, and fix.
